import {StyleSheet} from 'react-native';
import Colors from '../../../styles/Colors';

export default StyleSheet.create({
  container: {
    //flex: 1,
    alignItems: 'center',
  },
  label: {
    fontSize: 16,
    color: Colors.white,
  },
  value: {
    fontSize: 36,
    color: Colors.white,
  },
});
